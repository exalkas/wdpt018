/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["m.media-amazon.com", "static.wikia.nocookie.net"],
  },
};

module.exports = nextConfig;
