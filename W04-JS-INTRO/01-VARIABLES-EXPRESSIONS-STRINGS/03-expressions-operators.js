// Expressions
// is piece of code that is being evaluated to a single value

// "5+5" is an expression
const total = 5 + 5;
const firstName = "John";
const lastName = "Doe";

const name = firstName + " " + lastName;
console.log("🚀 ~ name:", name);

const average = 3 + 4 + 5 + 4 / 4;
console.log("🚀 ~ average:", average);

const sum = 3 * 2 + 1 / 2 + 3;
console.log("🚀 ~ sum:", sum);

const someSign = "distance" + 100;
console.log("🚀 ~ someSign:", someSign);

const module1 = 9 % 3;
console.log("🚀 ~ module:", module1);

const random = Math.random() * 10;
const rounded = Math.round(random);
console.log("🚀 ~ random:", random, rounded);
